# Build output
/build/
/akcore
/agentkate

# Go
/core/akcore
*.test
*.out

# Editor / OS
*.sock
.cache/
*.o
*.user
compile_commands.json

# Agent worktrees (created at runtime under .agentkate/)
/.agentkate/

# Packaging / build outputs (scripts/package.sh, make-dist.sh)
/dist/
