# clangd configuration for Agent Kate.
#
# Qt6/KF6 propagate GCC-only machine flags via their CMake interface — notably
# -mno-direct-extern-access — and clangd parses with clang, which rejects them.
# Machine flags do not affect code analysis, so drop every -m* flag.
CompileFlags:
  Remove: [-m*]
